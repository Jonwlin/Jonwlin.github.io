// Homepage heading treatment (Cabin, bold, uppercase, 1px tracking) — the
// ported equivalent of the old `h1..h6` rule from the Grayscale theme. Per-
// element margins/sizes are applied alongside this where the heading is used.
export const HEADING = "font-heading font-bold uppercase tracking-[1px]";
